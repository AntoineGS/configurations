---
description: "Turn the current conversation into a published specification"
---

Load the `to-spec` skill using the skill tool and follow its instructions. Apply it to the current conversation and these additional arguments, if any:

$ARGUMENTS
