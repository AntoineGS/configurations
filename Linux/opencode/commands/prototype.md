---
description: "Build a throwaway prototype to answer a design question"
---

Load the `prototype` skill using the skill tool and follow its instructions. Apply it to the current conversation and these additional arguments, if any:

$ARGUMENTS
