---
description: "Build or fix behavior with test-driven development"
---

Load the `tdd` skill using the skill tool and follow its instructions. Apply it to the current conversation and these additional arguments, if any:

$ARGUMENTS
