---
description: "Triage incoming issues and external pull requests"
---

Load the `triage` skill using the skill tool and follow its instructions. Apply it to the current conversation and these additional arguments, if any:

$ARGUMENTS
