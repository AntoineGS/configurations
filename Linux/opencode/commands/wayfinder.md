---
description: "Map and resolve decisions for a large multi-session effort"
---

Load the `wayfinder` skill using the skill tool and follow its instructions. Apply it to the current conversation and these additional arguments, if any:

$ARGUMENTS
