---
description: "Build and sharpen the project's domain model"
---

Load the `domain-modeling` skill using the skill tool and follow its instructions. Apply it to the current conversation and these additional arguments, if any:

$ARGUMENTS
