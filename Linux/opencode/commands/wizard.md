---
description: "Generate a wizard for steps that require a human"
---

Load the `wizard` skill using the skill tool and follow its instructions. Apply it to the current conversation and these additional arguments, if any:

$ARGUMENTS
