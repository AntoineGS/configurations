#!/bin/sh
# The polkit PAM transaction refreshes the Kerberos ticket before this runs.
if [ "$(/usr/bin/id -u)" != "${PKEXEC_UID:-}" ]; then
  printf '%s\n' 'refresh-office-shares-credentials: refusing a different target user' >&2
  exit 1
fi

exit 0
