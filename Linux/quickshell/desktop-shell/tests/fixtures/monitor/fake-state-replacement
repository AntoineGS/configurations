#!/bin/sh
set -eu

MONITOR_FIXTURE_STATE_OUTPUT="${MONITOR_FIXTURE_REPLACEMENT_STATE_OUTPUT:?}" \
MONITOR_FIXTURE_STATE_HOLD="" \
MONITOR_FIXTURE_STATE_RELEASE="" \
MONITOR_FIXTURE_STATE_LOG="${MONITOR_FIXTURE_STATE_LOG:-}" \
  exec "$(dirname "$0")/fake-state" "$@"
