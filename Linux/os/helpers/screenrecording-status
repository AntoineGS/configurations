#!/usr/bin/env bash
set -Eeuo pipefail

if pgrep -f '^gpu-screen-recorder' >/dev/null; then
  printf '%s\n' '{"text":"󰑋","tooltip":"Screen recording active","class":"recording"}'
else
  printf '%s\n' '{"text":"","tooltip":"","class":"idle"}'
fi
