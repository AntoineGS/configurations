#!/usr/bin/env bash
set -Eeuo pipefail

state="$(desktop-shell call desktop.notifications toggleDnd)"
case "$state" in
  enabled|disabled)
    printf '%s\n' "$state"
    ;;
  *)
    printf 'toggle-notification-silencing: unexpected notification state: %s\n' "$state" >&2
    exit 1
    ;;
esac
