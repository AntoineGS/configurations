#!/usr/bin/env bash
set -Eeuo pipefail

usage() {
  printf 'usage: toggle-desktop-shell-bar\n' >&2
}

(($# == 0)) || { usage; exit 2; }

exec quickshell ipc --any-display -p "$HOME/.config/quickshell/desktop-shell" call desktop-shell toggleBar
