{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "servers": {
      "home-assistant": {
        "type": "local",
        "command": [
          "sh",
          "-c",
          "HOMEASSISTANT_TOKEN=$(jq -er '.mcpServers[\"home-assistant\"].env.HOMEASSISTANT_TOKEN // empty' \"$HOME/.claude.json\") || exit 1; export HOMEASSISTANT_TOKEN; exec uvx --python 3.13 --refresh ha-mcp@latest"
        ],
        "environment": {
          "HOMEASSISTANT_URL": "https://ha.antoinedev.io"
        }
      }
    }
  }
}
